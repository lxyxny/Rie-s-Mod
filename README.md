# Rie-s-Mod
=== Commands === /name &lt;text> → Change your displayed name /level &lt;number> → Change your level (0-9999) /gradient &lt;preset> → Use preset: fire, ocean, sunset, neon, rainbow /gradient c1,c2,s → Custom gradient (2-6 colors + speed in ms) /info → Show this help 💡 Changes are visible only to other mod users.
